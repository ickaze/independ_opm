# 0.10b：参照資料と再作成結果

## 起点

0.9配布内容を起点に作成しました（ご指定の0.90を既存の0.9として扱っています）。旧LFOの時間カウンタ、波形・深度計算、関連テストと説明・更新履歴を取り除き、今回添付された原版ソースのlfo.hから再実装しました。過去に配布済みのZIPは変更していません。

## 原版ソースから採用した範囲

X68Sound/lfo.h の SetLFRQ、Update、LfoReset、波形テーブルの初期化、深度計算を確認しました。

- FREQ上位4bitをH、下位4bitをLとする。
- H<15：イベント間隔は8×2^(15−H)内部サンプル、4bit小数部への加算は16+L。
- H=15：イベント間隔は16内部サンプル、加算は2×(16+L)。
- イベント時に小数部から繰り上がった整数分だけ位相を進め、余りを保持。
- 512位置。鋸歯状波・矩形波は256位置ごとの繰返し、三角波は位相の進行量を2倍にする。
- 18h書込みは同じ値でも時間カウンタをリセット。位相と小数部は保持。
- 01h bit1は位相を0にして停止。部分的に進んだ時間カウンタと小数部は保持。
- 矩形波AMは最大256。inspect_lfo().phase/amはuint16_tへ拡張。

原版の62500Hz基準を、当コアではclock/64の内部サンプルに対応させました。4MHzで一致し、それ以外はクロックに比例する適応です。

ランダム生成器はユーザー指定の17bit・bit0/bit3タップを維持。波形3のイベントごとに一度進め、下位9bitを位相にして鋸歯状のAM/PMへ写像します。この接続は実機未検証で、原版のirnd()とは異なります。低位ニブルは周期波形の平均速度に反映されますが、同じ上位ニブルのランダム更新間隔には影響しません。

PMD/AMDの整数深度を既存のApplication Manualに基づくチャンネル感度のdB/cents近似へ渡します。原版の全チャンネル量子化・オペレータ演算まで一致させた移植ではありません。

## 検証

原版lfo.hを一時ディレクトリでコンパイルし、新実装と256周波数×周期波形3種類×32更新イベントを比較しました。原版のAM/PM値と比較が通過しています。この比較は原版と同じチャンネル倍率を検査側で適用しており、当コアのdB/cents変換を含めた音声一致ではありません。原版ソース自体はパッケージに同梱しません。

```
python tests/run_reference_check.py /path/to/X68Sound_src020615.zip
```

Python3とg++が必要です。通常のcore/precision/output_timingテストもGCCで通過。周波数書込み、同値書込み、リセット停止と再開、矩形AMの256、状態保存・刻み幅も検証しました。Windows/MSVCは未検証です。同梱Linux実行ファイルとWAVは本版から再生成しました。

## ライセンスの判断

添付原版readme.txtには、改変、改変物の公開、組込みおよび配布が自由で、作者への報告不要という許諾があります。そのため今回のソース参照・再実装を進めました。一方、著作権の放棄やCC0への変更を明記した条項はありません。

計算式・動作の事実そのものとコードの創作的表現は区別されるため、ソースを見たという一点でCC0が必ず不可能になるわけではありません。ただし今回はソースを参照した再実装であり、全体に第三者の権利が残らないと断定する根拠は得られていません。CC0の適用条件を満たしたとは判断せず、LICENSE.txtは全体CC0に変更していません。原版の許諾文をTHIRD_PARTY_NOTICES.txtへ収録しています。

CC0の説明：https://creativecommons.org/publicdomain/zero/1.0/
